import 'package:flutter/material.dart';

/// Palette matching the FinalYearProject / Mumbai Local design references.
abstract final class FypColors {
  /// App bar lavender (`#9581F1`).
  static const Color appBarLavender = Color(0xFF9581F1);

  /// Main home body dark indigo (`#3F3D8C`).
  static const Color homeBackground = Color(0xFF3F3D8C);

  /// Ticket card shell — light pink.
  static const Color ticketCardPink = Color(0xFFFFE4EC);

  /// “Ticket Booking” header strip — cyan (`#4CDAF1`).
  static const Color ticketHeaderCyan = Color(0xFF4CDAF1);

  /// Primary yellow for bottom nav & grid tiles (`#FFFF00`).
  static const Color accentYellow = Color(0xFFFFFF00);

  /// Neon green NEXT button (`#00FF00`).
  static const Color nextGreen = Color(0xFF00FF00);

  /// Icons / labels on yellow tiles — deep blue.
  static const Color iconBlue = Color(0xFF0D47A1);

  /// Profile / secondary panels.
  static const Color profileDeepBlue = Color(0xFF1565C0);

  static const Color white = Color(0xFFFFFFFF);

  static const Color black = Color(0xFF000000);

  /// Inbox / soft app backgrounds.
  static const Color inboxBackground = Color(0xFF8E97FD);

  /// Schedule screen sky blue (`#5D9CEC`).
  static const Color scheduleBackground = Color(0xFF5D9CEC);

  /// Form screen charcoal (`#333333`).
  static const Color formBackground = Color(0xFF333333);

  /// Schedule card cyan center (`#00CFFF`).
  static const Color scheduleCyan = Color(0xFF00CFFF);
}
